// DualSense DJ — Mixxx MIDI Scripting Stub
// Extend this file to add scripted behaviors (e.g. pitch nudge scaling, effect cycling)
var DualSenseDJ = {};

DualSenseDJ.init = function(id, debugging) {
    // Called when the mapping is loaded in Mixxx
};

DualSenseDJ.shutdown = function(id) {
    // Called when the mapping is unloaded in Mixxx
};
